import * as React from 'react';
import { Route, Routes } from 'react-router-dom';

const AppRouter = () => {
    return (
        <Routes>
            {/* Add routes here */}
        </Routes>
    );
};

export default AppRouter;