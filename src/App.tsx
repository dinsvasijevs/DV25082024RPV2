import * as React from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import './App.css';

const App = () => {
    return (
        <BrowserRouter>
            <Routes>
                {/* Add routes here */}
            </Routes>
        </BrowserRouter>
    );
};

export default App;